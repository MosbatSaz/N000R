

Notice

        this components load in `Drawer` ( Navigation-drawer.vue load in App.vue )
        ```
            + Sign-in.vue
            + Sign-up.vue
            + User-panel.vue
        ```
        
        this components load in `Dialog` ( Dialog placed in the Home.vue ) 
        ```
            + Advicer.vue
            + Archive.vue
            + Classes.vue
            + Konkur.vue
            + Sign-in.vue
            + Sign-in.vue   
        ```

        this components load in `Bottom Sheet` ( Dialog placed in the Home.vue ) 
        ```
            + Ceo.vue
            + Contact.vue
            + Faq.vue
            + Comment.vue 
        ```